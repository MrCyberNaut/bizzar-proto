// app/ar/page.tsx
// No Script tag — MindAR loaded inside component via type="module" injection

import { ARPageClient } from './ARPageClient'

export default function ARPage() {
  return <ARPageClient />
}
